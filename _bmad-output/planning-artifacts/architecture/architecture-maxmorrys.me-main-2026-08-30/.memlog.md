---
scope: Migration intégrale de la plateforme maxmorrys.me vers le design system Max-Morrys + PWA installable + deux apps natives Expo/React Native
purpose: Fixer les invariants partagés pour que le socle DS, les quatre surfaces web, la PWA et les apps natives ne divergent pas
altitude: initiative
updated: 2026-08-31T12:56
---

- (constraint) Sources de vérité imposées : Design_System_Max-Morrys/{readme.md,design_handoff_maxmorrys/README.md,REGLES-DE-REVUE.md,tokens/,brand/,components/,ui_kits/,uploads/prd.md}. Le DS déclare que les kits gagnent contre toute convention de bibliothèque : « si le kit dit 5 px, c'est 5 px ».
- (constraint) Six règles de revue bloquantes (REGLES-DE-REVUE.md) : 1 flou réservé au chrome fixe, 2 plancher d'opacité du verre par jeton, 3 transform+opacity seulement, 4 replis déclarés une seule fois globalement, 5 repli .lowfi qui connaît le thème, 6 monospace = nombre sourcé. Chacune corrige un défaut OBSERVÉ, pas anticipé.
- (constraint) Points ouverts du handoff B et C : les composants du DS n'ont AUCUN champ de formulaire réel (Field rend un div, zéro input/textarea/select dans tout le système) et presque rien n'est atteignable au clavier (a sans href, span sans tabindex). Déclarés bloquants en production par le DS lui-même.
- (constraint) Point ouvert A du handoff, NON RÉSOLU : sur la pile réelle (lobe -> voile 42% -> verre), l'encre tertiaire #98A1AE tombe à 1,48:1 et l'encre secondaire #5A6472 à 3,4:1 ; en nuit le secondaire #A2ADBB à 2,11:1. Le DS écrit : le statu quo n'est pas conforme, le choix appartient au produit.
- (version) Dépôt existant vérifié le 30/08/2026 : React 18.3.1, Vite 5.4.2, Tailwind 3.4.1 (config JS), React Router 7.13.1 (data router), TypeScript 5.5.3, Firebase JS SDK 12.9.0, i18next 26.3.2, framer-motion 12.38.0, Vitest 2.1.9. Aucune dépendance Expo/React Native/Capacitor présente.
- (version) Volumétrie mesurée : 252 fichiers TS/TSX, 42 454 lignes, 1 979 occurrences de classes de couleur héritées dans 140 fichiers, 3 015 occurrences de variant dark: dans 141 fichiers, 31 fichiers mentionnant Rysmo.
- (direction) Le porteur a tranché : (1) livrer la PWA du DS ET de vraies apps natives Expo/React Native ; (2) appliquer le renommage complet Hello!/Rysmo/Répétiteur ; (3) migrer en une seule passe.
- (decision) AD-1 Les jetons sont copiés verbatim, jamais retapés. Binds: tout. Prevents: la dérive valeur par valeur entre le kit et le code. Rule: src/design-system/css/** est une copie littérale de Design_System_Max-Morrys/{tokens,brand}/** ; un changement se fait dans le DS puis se resynchronise par npm run ds:sync.
- (decision) AD-2 Tailwind lit les jetons, il ne les redéfinit pas. Binds: tailwind.config.js + tous les TSX. Prevents: deux palettes concurrentes (47 échelles héritées contre les jetons DS). Rule: theme.extend.colors ne contient que des var(--…) ; les échelles brand/accent/plum/morrys/lagoon/coral/teal sont supprimées ; aucun hex dans un TSX.
- (decision) AD-3 Le mode sombre est une portée CSS .dk, jamais une prop, jamais un dark: de couleur. Binds: tout. Prevents: le défaut à 1,4:1 — un composant qui retombe silencieusement sur sa valeur claire dans douze écrans à la fois. Rule: html porte class dk + data-mm-dark ; darkMode: ['selector','.dk'] ; dark: autorisé pour la MISE EN PAGE seulement ; aucun composant n'accepte de prop dark ni theme.
- (decision) AD-4 Le flou n'a droit qu'au chrome en position fixe, et la machine le vérifie. Binds: tous composants et écrans. Prevents: la reconstitution du budget dépassé (21 surfaces mesurées sur la page Club). Rule: backdrop-filter n'existe que dans brand/surfaces.css sur .glass ; .glass ne s'applique qu'à un élément fixed|sticky ; npm run ds:check échoue sinon ; tout le reste est .glass-flat/.glass-hero/.glass-d/.truth.
- (decision) AD-5 Un nombre en monospace porte sa source dans le TYPE. Binds: tous les affichages chiffrés. Prevents: le chiffre de façade non sourcé que le PRD a constaté contredit par la base de production. Rule: le composant Num exige source: 'db'|'server'|{cite} et asOf ; c'est le seul chemin vers --f-mono pour un chiffre ; sans source, l'état vide s'affiche à la place.
- (decision) AD-6 Les primitives sont des éléments natifs, focalisables, avec de vrais contrôles. Binds: forms, actions, navigation. Prevents: les points ouverts B et C reportés en production. Rule: Field rend input/textarea/select avec label for ; les boutons rendent button ; toute cible rend a href ou button ; cible tactile >= 44 px via .mm-touch-extend ; anneau double sur :focus-visible.
- (decision) AD-7 Les trois replis sont déclarés une seule fois, et connaissent le thème. Binds: design-system/css. Prevents: la seconde déclaration locale qui gagne par ordre de chargement et rétablit ce que la personne a explicitement refusé. Rule: prefers-reduced-motion, prefers-reduced-transparency et @supports not(backdrop-filter) n'existent que dans brand/fallback.css, chacun décliné en clair ET en .dk ; ds:check refuse toute autre occurrence.
- (decision) AD-8 Une seule source de jetons pour trois plateformes : CSS d'abord, TS généré. Binds: web, PWA, natif. Prevents: la palette qui dérive entre web et natif, React Native n'ayant ni CSS ni backdrop-filter. Rule: scripts/ds-tokens.mjs lit design-system/css/tokens/*.css et émet design-system/tokens.generated.ts ; le natif n'importe que ce fichier ; il est généré, jamais édité ; ds:check échoue s'il est désynchronisé.
- (decision) AD-9 Le dépôt ne devient PAS un monorepo, src/ ne bouge pas. Binds: arborescence, build, déploiement. Prevents: casser la chaîne qui déploie réellement (vite -> dist -> firebase hosting -> origin-pull Cloudflare) pour un gain d'organisation. Rule: le web reste src/ (alias @) ; le DS porté vit dans src/design-system/ (alias @ds) ; le natif vit dans mobile/, projet Expo autonome ; aucun champ workspaces ajouté à la racine.
- (decision) AD-10 Le natif ne reproduit pas le verre ni le maillage à l'identique, il en porte l'équivalent déclaré. Binds: mobile/. Prevents: la course au pixel sur une plateforme sans backdrop-filter. Rule: maillage = trois RadialGradient SVG figés ; verre = expo-blur BlurView UNIQUEMENT sur le chrome fixe, ailleurs un View opaque au voile de --glass-a-flat ; toutes les valeurs viennent de tokens.generated.ts.
- (decision) AD-11 Le paiement natif ne passe pas par les magasins. Binds: mobile/, tunnel d'achat. Prevents: les 15 à 30 % de commission et la disparition de Wave/Orange Money — la raison même pour laquelle le DS excluait le natif. Rule: aucun achat in-app ; le tunnel natif s'arrête à la sélection du moyen et ouvre l'URL de paiement web dans le navigateur système.
- (assumption) AD-11 suppose que la revue Apple accepte le renvoi navigateur au titre de l'App Store Review Guideline 3.1.1. Non vérifié. À trancher avant toute soumission ; le repli connu est de retirer tout achat de l'app native et de la cantonner à la consultation.
- (decision) AD-12 Le renommage a une seule source, lue par tous. Binds: 31 fichiers Rysmo, i18n, routes. Prevents: un écran qui écrit le nom en dur et casse le renommage sans que rien ne le signale. Rule: Hello! = mot-symbole web (Wordmark variant hello) ; Rysmo = APP_NAME ; le tuteur lit tutorName(profile), défaut Répétiteur, surchargé par users/{uid}.tutorName ; route /mon-espace/repetiteur avec redirection depuis /mon-espace/rysmo.
- (decision) AD-13 Les titres d'affichage sont ÉCRITS par langue, jamais traduits. Binds: i18n. Prevents: le titre français qui se replie tout seul et perd sa masse (le français court ~18 % plus long). Rule: toute clé de titre d'affichage est un tableau de lignes rendu ligne par ligne ; aucun repli automatique ; les six libellés de navigation sont écrits, pas traduits.
- (decision) AD-14 La colonne de lecture ne s'élargit jamais. Binds: prose, article, leçon. Prevents: la seule règle de mise en page que le DS déclare non négociable. Rule: tout conteneur de prose porte max-width: var(--measure-prose) (68ch), à 390 px comme à 1400 ; l'espace gagné va à la marge.
- (decision) AD-15 Les routes bilingues sont une table unique vérifiée à l'unicité au chargement. Binds: routing. Prevents: deux segments anglais qui collident produisent une page inatteignable dans une langue, SANS erreur de compilation pour le signaler. Rule: table dans un module unique, assertion d'unicité au chargement, échec dur en dev et journalisation en prod.
- (decision) AD-16 Le mouvement ne porte que transform et opacity. Binds: tout. Prevents: la ré-introduction du coût de recomposition sur le profil d'appareil visé (2 Go, 4 coeurs). Rule: exception unique déjà écrite (barfill, .prog-fill) ; ds:check grep les transitions/animations sur width|height|top|left|margin|padding|inset et échoue.
- (decision) AD-17 La PWA n'interrompt jamais et son seul argument est le forfait. Binds: PWA. Prevents: la modale d'installation qui interrompt exactement ce qu'on venait faire. Rule: bandeau bas après la deuxième visite, jamais de modale ; le libellé parle hors connexion, jamais « installe notre app » ; le centre de notifications est le seul canal sortant, cinq types, aucun e-mail promis.
- (question) Point A du handoff : l'encre tertiaire tombe à 1,48:1 sur le fond réel. Deux issues nommées par le DS — remonter le voile en haut d'écran, ou assombrir les encres secondaire et tertiaire. Décision produit requise ; le statu quo n'est pas conforme.
- (event) memlog initialisé et décisions de cadrage consignées
- (event) prêt à distiller
- (event) spine distillée depuis le memlog (17 AD, conventions, pile, amorce, différé, 1 question bloquante)
- (direction) Le porteur tranche le point A : remonter le voile en haut d'écran, garder les encres.
- (constraint) MESURÉ 30/08/2026 (script WCAG sur la pile lobe->voile->verre, 5 lobes x 3 verres) : voile 0.42 -> ink-2 3,93:1 pire cas ; voile 0.60 -> ink-2 4,51:1 CONFORME. Le modèle reproduit les chiffres du handoff (ink-3 1,71-2,14 contre 1,48 annoncé ; ink-2 3,93-4,20 contre 3,4).
- (constraint) MESURÉ : --ink-3 #98A1AE fait 2,61:1 SUR BLANC PUR. Il échoue avant que le maillage existe. Plafond à voile 0.94 : 2,51:1. Aucune valeur de voile ne le rend conforme — le levier est le jeton, pas le voile.
- (constraint) MESURÉ, non signalé par le handoff : en sombre le voile seul ne suffit pas non plus pour --ink-2 #A2ADBB (2,15:1 à 0.42 ; 3,89:1 à 0.70). La nuance qui résout : le voile est un DÉGRADÉ, .42 ne vaut qu'en haut d'écran où seul vit le titre d'affichage Fraunces 900 41-74 px — grand texte, seuil 3:1, tenu à 4,28:1.
- (decision) AD-18 Le voile de lisibilité remonte, et --ink-3 cesse de porter du texte. Binds: tokens, brand/mesh.css, toutes les surfaces. Prevents: le point A du handoff — l'encre secondaire sous 4,5:1 et l'encre tertiaire sous 3:1 sur le fond réel, que le DS déclare non conforme. Rule: voile clair 0.60/0.78/0.90 (au lieu de 0.42/0.72/0.90) ; voile nuit 0.62/0.86/0.94 (au lieu de 0.42/0.78/0.94) ; --ink-3 et --text-faint ne portent JAMAIS de texte — ils restent aux filets, puits d'icônes, points d'étape et à l'état désactivé, exempté par WCAG 1.4.3 ; tout texte aujourd'hui sur --text-faint remonte sur --ink-2 ; ds:check refuse color:var(--ink-3) et var(--text-faint) hors d'une règle :disabled.
- (event) point A tranché et mesuré ; AD-18 ajoutée
- (event) spine finalized
- (decision) AD-19 Les trois couleurs d'état ont aussi une version nuit. Binds: tokens, tous les messages d'état. Prevents: le même piège que les quatre teintes, une porte plus loin — dark.css redéclare les teintes de marque et oublie --ok/--warn/--stop. MESURÉ sur #0B0E13 : ok #0F7B52 3,66:1, warn #8A4B00 2,84:1, stop #B4231F 2,95:1, tous sous 4,5:1. Rule: .dk redéclare --ok #2BD18B (9,75:1), --warn #FFB24D (10,79:1, valeur que --mm-orange-t prend déjà en nuit), --stop #FF8A80 (8,47:1, valeur que --error-ring portait déjà). Deux des trois n'étaient pas un choix : le DS les avait déjà retenues ailleurs sans faire le lien.
- (decision) AD-20 Le corail a aussi une version texte. Binds: entrée agence de la barre haute, tout texte corail. Prevents: le DS donne au corail un rôle de TEXTE (« entrée en corail » pour l'agence, hors des quatre verbes) sans fournir de teinte lisible. MESURÉ : #FF6E7F fait 2,70:1 sur blanc, entre l'orange (2,47) et le teal (2,84), donc du même côté de la ligne qu'eux — mais lui n'a pas de variante -t. Rule: --mm-corail-t #C22A3C (5,69:1 sur blanc) en clair ; sous .dk il pointe sur #FF6E7F (7,17:1), même schéma que --mm-orange-t et --mm-teal-t.
- (constat) AD-8 avait un trou, trouvé en portant le natif : scripts/ds-tokens.mjs ne lisait que css/tokens/ et pas css/overrides/. Conséquence — les jetons des écarts délibérés n'atteignaient jamais tokens.generated.ts, et l'application native aurait embarqué les valeurs d'AVANT chaque correction, dont le vert d'état à 3,66:1 sur fond nuit corrigé au web par AD-19. C'est exactement la dérive qu'AD-8 existe pour empêcher : pas une palette qui change d'un coup, mais une valeur à la fois, dans la plateforme que personne ne regarde pendant qu'on corrige l'autre. Corrigé : les overrides sont lus après tokens/, dans l'ordre de la cascade. Second défaut du même script : la regex d'extraction exigeait une accolade fermante en début de ligne, donc ratait SILENCIEUSEMENT tout bloc écrit sur une seule ligne. 206 -> 211 jetons.
- (decision) AD-8 précisée : le fichier de jetons généré lit tokens/ PUIS overrides/, dans l'ordre de la cascade de styles.css. Un jeton déclaré dans un écart délibéré fait partie du système au même titre qu'un jeton du kit — sinon les deux plateformes divergent d'une correction à la fois.
- (decision) Le prix barré 295 000 de Présence Digitale EST sourcé, contre la lecture du plan. Le readme du kit écrit « le prix réellement pratiqué est 250 000, pas 295 000 » ; lu seul, cela interdit le barré. Mais src/lib/presence/offer.ts porte price: 295_000 ET promoPrice: 250_000 : le readme nomme le prix ENCAISSÉ, il ne nie pas le prix de liste. C'est exactement la structure de PriceBlock (amount = ce qu'on paie, strike = ce qu'on ne paie plus). Les deux valeurs sortent du catalogue en source=db. Un pack sans promoPrice n'a pas de barré.
- (constraint) PACKS[0].promoPrice est ignoré par computeTotals() : la page d'offre affiche 250 000 barré 295 000, tandis que le devis partageable recalcule 295 000. Le montant vu à l'offre et le montant vu au devis divergent. Non corrigé — cela touche ce qu'un client a sous les yeux au moment de payer. À trancher humainement.
- (decision) Icon.tsx passe de 35 à 42 glyphes : gift, copy, handshake, trophy, crown, medal, video, empruntés à Lucide et déclarés, en plus de heart et repeat. Cela met fin à la cohabitation lucide-react / @phosphor-icons/react que les onze onglets du Club portaient. CircleNotch n'a délibérément pas de remplaçant (Button : « jamais de rond qui tourne ») ; XLogo non plus (marque tierce, pas icône d'interface).
- (decision) captureError() renvoie désormais l'identifiant d'événement Sentry. C'est la « référence de l'incident » que la maquette d'erreur affiche : la seule chose qu'une personne bloquée puisse recopier au support. Sans DSN il n'y en a pas, et l'écran le dit au lieu d'en inventer une.
- (decision) PageLoader (rond animate-spin, repli des 60 routes paresseuses, plus un dark: de couleur) devient PageSkeleton, à la géométrie du contenu. Motif : « quand le contenu arrive, rien ne saute ». Le squelette n'enveloppe pas dans PageSite — le même repli sert le site, la console et l'espace apprenant.
- (decision) ErrorBoundary recomposée sur motif → conséquence → sortie. Le texte reste écrit dans le composant, sans i18next : la frontière s'affiche quand l'arbre React est tombé, et i18next peut en faire partie. La langue est lue sur <html lang>. Le retour à l'accueil est un vrai <a href> et non le routeur, qui fait partie de ce qui vient peut-être de tomber.
- (decision) Le motif de console est écrit une fois dans src/components/console/ pour les 19 écrans, conformément au kit qui n'en dessine que 5 et fournit les 14 autres comme pipelines de statuts seulement. Trois zones imposées : filtre par statut jamais par date, liste dense à une action par ligne, pied nommant les angles morts.
- (constraint) L'échappatoire ok-ds n'acceptait que la forme // ok-ds. Entre deux balises JSX, // n'est pas un commentaire : un logo de marque tierce ne pouvait donc pas la porter sur la ligne du path fill=#… qu'elle doit couvrir. Élargie à /* ok-ds */.
- (constraint) SÉCURITÉ — firestore.rules autorisait l'auto-inscription à N'IMPORTE QUELLE formation. La règle disait « free courses » en commentaire et ne vérifiait que : connecté, s'inscrire soi-même, format d'identifiant. Le prix n'était jamais lu. N'importe quel compte pouvait écrire enrollments/{sonUid}_{formationPayante} et obtenir l'accès complet sans transaction. Le garde isFree existait, mais dans Checkout.tsx — donc chez la personne qui contourne. Fermé par isFreeFormation(), qui calque le prédicat client (promoPrice ?? price) == 0 et exige status == published. Vérifié sans risque de régression : les inscriptions payantes naissent côté serveur (payment.ts, admin.ts) via le SDK Admin, qui contourne les règles ; le seul chemin client est la branche gratuite de Checkout. NON TESTÉ : l'émulateur Firestore exige un JDK 21 absent de la machine, les 43 tests de règles n'ont pas pu tourner.
- (constraint) Deux écarts montant affiché / montant débité, signalés et NON corrigés. (1) Le coupon n'est jamais déduit du total affiché : Checkout lit promoPrice ?? price dans le navigateur, createBictorysCharge débite finalPrice - couponDiscount. (2) computeTotals() de Présence Digitale ignore promoPrice : la page d'offre affiche 250 000 barré 295 000, le devis partageable recalcule 295 000. Les deux touchent ce qu'un client voit au moment de payer.
- (decision) ConsolePage ne rend plus de titre visible : AppShell affichait déjà le même en <h1> depuis ADMIN_TITLES, ce qui donnait deux <h1> par écran sur les dix-neuf. Le titre devient le nom accessible de la région.
- (decision) Six témoignages fabriqués, signés de noms de personnes, étaient injectables en un clic dans la collection de production en status approved / featured / rating 5. src/data/testimonials.ts et son bouton d'import sont supprimés.
