{
  "id": "774G38cqIvZsJUHm",
  "name": "WF-SOCIAL-04 — Visuels (fond IA Gemini 2.5 Flash Image)",
  "active": true,
  "nodes": [
    {
      "parameters": {
        "rule": {
          "interval": [
            {
              "field": "cronExpression",
              "expression": "0 8-20/2 * * *"
            }
          ]
        }
      },
      "type": "n8n-nodes-base.scheduleTrigger",
      "typeVersion": 1.2,
      "position": [
        0,
        0
      ],
      "id": "cron-cron---toutes-2h-8-20h",
      "name": "Cron — Toutes 2h 8-20h"
    },
    {
      "parameters": {
        "authentication": "nocoDbApiToken",
        "workspaceId": {
          "__rl": true,
          "value": "wpaxkizo",
          "mode": "id"
        },
        "projectId": {
          "__rl": true,
          "value": "ph7ugup4mggzj2y",
          "mode": "id"
        },
        "table": {
          "__rl": true,
          "value": "m3wim4coagaoot7",
          "mode": "id"
        },
        "resource": "row",
        "operation": "search",
        "returnAll": false,
        "limit": 2,
        "options": {
          "where": "(Status,eq,rédigé)"
        }
      },
      "type": "n8n-nodes-base.nocoDb",
      "typeVersion": 4,
      "position": [
        224,
        0
      ],
      "id": "at-s-airtable---lire-rédigésn",
      "name": "Airtable — Lire rédigés (NocoDB)",
      "credentials": {
        "nocoDbApiToken": {
          "id": "MMNOCODBCRED001",
          "name": "NocoDB Max-Morrys"
        }
      }
    },
    {
      "parameters": {
        "jsCode": "// Aplatit la sortie NocoDB v3 {id, fields:{}} au format plat du noeud\n// Airtable historique : {...champs, id} -> aucune expression aval a changer.\nreturn $input.all().map(i => ({ json: { ...(i.json.fields || {}), id: i.json.id } }));"
      },
      "id": "at-s-airtable---lire-rédigésf",
      "name": "Airtable — Lire rédigés",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        414,
        0
      ]
    },
    {
      "parameters": {
        "jsCode": "// Découpe chaque contenu rédigé en cartes à rendre — une par slide.\nconst MAX_CARTES = 10;\n\nfunction lireCartes(raw) {\n  let v = raw;\n  if (typeof v === 'string') {\n    if (!v.trim()) return [];\n    try { v = JSON.parse(v); } catch (e) { return []; }\n  }\n  if (!Array.isArray(v)) return [];\n  const out = [];\n  for (const s of v) {\n    const o = (s && typeof s === 'object') ? s : { titre: String(s == null ? '' : s) };\n    const titre = String(o.titre || '').trim();\n    if (!titre) continue; // une carte sans titre ne se rend pas : autant la jeter ici\n    const carte = { titre: titre };\n    if (String(o.corps || '').trim()) carte.corps = String(o.corps).trim();\n    if (String(o.cta || '').trim()) carte.cta = String(o.cta).trim();\n    if (Array.isArray(o.options)) {\n      const opts = o.options.map(x => String(x == null ? '' : x).trim()).filter(Boolean).slice(0, 4);\n      if (opts.length >= 2) carte.options = opts; // un sondage à une seule réponse n'en est pas un\n    }\n    out.push(carte);\n    if (out.length >= MAX_CARTES) break;\n  }\n  return out;\n}\n\nconst out = [];\nfor (const item of $input.all()) {\n  const r = item.json || {};\n  let cartes = lireCartes(r.Slides_JSON);\n  const fmt = String(r.Format_Post || '').toLowerCase();\n  // Hors carrousel, un seul visuel : aucun autre format n'en publie plusieurs.\n  if (fmt !== 'carrousel' && cartes.length > 1) cartes = [cartes[0]];\n  // Repli sur le Titre : un post rédigé avant ce changement n'a pas de `Slides_JSON`. Il doit\n  // quand même sortir une carte — bloquer la file serait pire que rendre le titre seul.\n  if (!cartes.length) cartes = [{ titre: String(r.Titre || 'Max-Morrys').trim() || 'Max-Morrys' }];\n  for (let i = 0; i < cartes.length; i++) {\n    out.push({ json: { id: r.id, carte: cartes[i], image_index: i + 1, total_images: cartes.length } });\n  }\n}\nreturn out;"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        800,
        0
      ],
      "id": "code-parse---stratégie-visuelle",
      "name": "Parse — Cartes"
    },
    {
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "// Compose la charge du moteur de rendu, une par créa. Le TEXTE vient du rédacteur (`carte`),\n// la MISE EN FORME (gabarit, format, accent, eyebrow) des champs du contenu.\nlet RENDER_CARD_URL = '', RENDER_KEY = '';\ntry {\n  const rc = $('Airtable — Lire Config Render').all().map(i => i.json);\n  const g = (kk) => { const r = rc.find(x => (x.fields ? x.fields.Cle : x.Cle) === kk); return r ? (r.fields ? r.fields.Valeur : r.Valeur) : ''; };\n  RENDER_CARD_URL = g('RENDER_CARD_URL'); RENDER_KEY = g('RENDER_KEY');\n} catch (e) {}\n\n// Les champs du post, indexés par id : chaque créa retrouve SON contenu. L'appariement par\n// position, longtemps utilisé ici, croisait deux posts dès qu'un item se perdait en route.\nconst parId = {};\ntry {\n  for (const r of $('Airtable — Lire rédigés').all()) {\n    const f = r.json.fields || r.json;\n    parId[r.json.id || f.id] = f;\n  }\n} catch (e) {}\n\nconst fmtMap = { story:'9:16', reel:'9:16', short:'9:16', carrousel:'4:5', post:'4:5', community_post:'1:1', thread:'1:1', live:'9:16' };\n// Accent = couleur du Pilier, choisie pour rester lisible sur le bleu profond du fond.\nconst accMap = { 'Éducation':'turquoise', 'Inspiration':'violet', 'Produit':'orange', 'Autorité':'corail', 'Communauté':'vert', 'Autre':'orange' };\n\nreturn $input.all().map(item => {\n  const b = item.json || {};\n  const f = parId[b.id] || {};\n  const carte = b.carte || {};\n  const fp = String(f.Format_Post || '').toLowerCase();\n  const format = fmtMap[fp] || '4:5';\n\n  // Le FORMAT décide du gabarit. Les gabarits plus riches (stat, checklist, versus, testimonial)\n  // réclament des données que cette chaîne n'a pas — un chiffre, deux colonnes : c'est le Designer\n  // qui les appelle sur ticket. Ici on ne produit que ce qu'on peut garantir.\n  let template = 'poster';\n  if (fp === 'story') template = 'ask';\n  else if (fp === 'carrousel') template = 'slide';\n\n  const idx = b.image_index || 1;\n  const total = b.total_images || 1;\n  const title = String(carte.titre || f.Titre || 'Max-Morrys').slice(0, 120);\n  // Le mot surligné est le dernier mot long du titre : celui sur lequel l'œil s'arrête.\n  const mots = title.replace(/[^\\wÀ-ÿ\\s]/g, ' ').split(/\\s+/).filter(x => x.length > 3);\n\n  const payload = {\n    template: template,\n    format: format,\n    title: title,\n    eyebrow: f.Serie || f.Pilier || '', // la série rend le rendez-vous reconnaissable\n    highlight: mots.length ? mots[mots.length - 1] : '',\n    accent: accMap[f.Pilier] || 'orange',\n    curve: false\n  };\n  if (carte.corps) payload.body = String(carte.corps).slice(0, 240);\n  if (carte.cta) payload.cta = String(carte.cta).slice(0, 40);\n  if (template === 'ask' && Array.isArray(carte.options)) payload.options = carte.options;\n  if (template === 'slide') {\n    // La cover promet, l'outro récapitule, le reste porte une idée.\n    payload.slideRole = idx === 1 ? 'cover' : (idx >= total ? 'outro' : 'body');\n    payload.slideIndex = idx;\n    payload.slideTotal = total;\n  }\n\n  return { json: { id: b.id, image_index: idx, total_images: total, carte: carte,\n                   render_url: RENDER_CARD_URL, render_key: RENDER_KEY, payload: payload } };\n});"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1696,
        0
      ],
      "id": "code-build---url-publique",
      "name": "Build — URL publique"
    },
    {
      "parameters": {
        "jsCode": "// Regroupe les créas par contenu et décide du statut.\n// Un post à qui il manque une image ne part PAS en validation : il attend en `image_needed`,\n// visible dans la base, plutôt que d'arriver sur Telegram sans visuel — ou en carrousel amputé —\n// puis d'échouer à la publication (Instagram refuse un post sans média).\nconst items = $input.all().map(i => i.json);\nlet mode = 'manual';\ntry {\n  const cfg = $('Airtable — Lire VALIDATION_MODE').all().map(i => i.json);\n  if (cfg.length && cfg[0].Valeur) mode = String(cfg[0].Valeur).trim().toLowerCase();\n} catch (e) {}\nconst statutComplet = mode === 'auto' ? 'validé' : 'prêt_à_valider';\n\nconst parId = {};\nfor (const it of items) {\n  if (!parId[it.id]) parId[it.id] = { id: it.id, urls: [], cartes: [], erreurs: [], total: it.total_images || 1 };\n  const g = parId[it.id];\n  const rang = (it.image_index || 1) - 1;\n  g.urls[rang] = it.public_url || '';\n  g.cartes[rang] = it.carte || null;\n  if (!it.public_url) g.erreurs.push('#' + (it.image_index || 1) + ' ' + (it.erreur || 'rendu indisponible'));\n}\n\nreturn Object.values(parId).map(g => {\n  const urls = g.urls.filter(Boolean);\n  const complet = urls.length === g.total;\n  return { json: {\n    id: g.id,\n    Status: complet ? statutComplet : 'image_needed',\n    Visuels_URLs: JSON.stringify(urls),\n    Visual_Strategy: JSON.stringify({ cartes: g.cartes.filter(Boolean),\n                                      rendus: urls.length + '/' + g.total,\n                                      erreurs: g.erreurs })\n  }};\n});"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1920,
        0
      ],
      "id": "code-collect---urls-par-contenu",
      "name": "Collect — URLs par contenu"
    },
    {
      "parameters": {
        "authentication": "nocoDbApiToken",
        "workspaceId": {
          "__rl": true,
          "value": "wpaxkizo",
          "mode": "id"
        },
        "projectId": {
          "__rl": true,
          "value": "ph7ugup4mggzj2y",
          "mode": "id"
        },
        "table": {
          "__rl": true,
          "value": "m3wim4coagaoot7",
          "mode": "id"
        },
        "resource": "row",
        "operation": "update",
        "dataToSend": "mapWithFields",
        "id": "={{ $json.id }}",
        "fieldsMapper": {
          "mappingMode": "defineBelow",
          "value": {
            "Status": "={{ $json.Status }}",
            "Visuels_URLs": "={{ $json.Visuels_URLs }}",
            "Visual_Strategy": "={{ $json.Visual_Strategy }}"
          },
          "matchingColumns": [],
          "schema": []
        }
      },
      "type": "n8n-nodes-base.nocoDb",
      "typeVersion": 4,
      "position": [
        2144,
        0
      ],
      "id": "at-u-airtable---status=prêt_à_val",
      "name": "Airtable — Status=prêt_à_valider",
      "credentials": {
        "nocoDbApiToken": {
          "id": "MMNOCODBCRED001",
          "name": "NocoDB Max-Morrys"
        }
      }
    },
    {
      "id": "vm-b413ecd3af7fn",
      "name": "Airtable — Lire VALIDATION_MODE (NocoDB)",
      "type": "n8n-nodes-base.nocoDb",
      "typeVersion": 4,
      "position": [
        224,
        200
      ],
      "parameters": {
        "authentication": "nocoDbApiToken",
        "workspaceId": {
          "__rl": true,
          "value": "wpaxkizo",
          "mode": "id"
        },
        "projectId": {
          "__rl": true,
          "value": "ph7ugup4mggzj2y",
          "mode": "id"
        },
        "table": {
          "__rl": true,
          "value": "mo6ubcgxi9x4u7d",
          "mode": "id"
        },
        "resource": "row",
        "operation": "search",
        "returnAll": true,
        "options": {
          "where": "(Cle,eq,VALIDATION_MODE)"
        }
      },
      "credentials": {
        "nocoDbApiToken": {
          "id": "MMNOCODBCRED001",
          "name": "NocoDB Max-Morrys"
        }
      },
      "continueOnFail": true
    },
    {
      "parameters": {
        "jsCode": "// Aplatit la sortie NocoDB v3 {id, fields:{}} au format plat du noeud\n// Airtable historique : {...champs, id} -> aucune expression aval a changer.\nreturn $input.all().map(i => ({ json: { ...(i.json.fields || {}), id: i.json.id } }));"
      },
      "id": "vm-b413ecd3af7ff",
      "name": "Airtable — Lire VALIDATION_MODE",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        414,
        200
      ]
    },
    {
      "id": "rc-render-cfg-01n",
      "name": "Airtable — Lire Config Render (NocoDB)",
      "type": "n8n-nodes-base.nocoDb",
      "typeVersion": 4,
      "position": [
        224,
        360
      ],
      "parameters": {
        "authentication": "nocoDbApiToken",
        "workspaceId": {
          "__rl": true,
          "value": "wpaxkizo",
          "mode": "id"
        },
        "projectId": {
          "__rl": true,
          "value": "ph7ugup4mggzj2y",
          "mode": "id"
        },
        "table": {
          "__rl": true,
          "value": "mo6ubcgxi9x4u7d",
          "mode": "id"
        },
        "resource": "row",
        "operation": "search",
        "returnAll": true,
        "options": {
          "where": "(Cle,eq,RENDER_CARD_URL)~or(Cle,eq,RENDER_KEY)"
        }
      },
      "credentials": {
        "nocoDbApiToken": {
          "id": "MMNOCODBCRED001",
          "name": "NocoDB Max-Morrys"
        }
      },
      "continueOnFail": true
    },
    {
      "parameters": {
        "jsCode": "// Aplatit la sortie NocoDB v3 {id, fields:{}} au format plat du noeud\n// Airtable historique : {...champs, id} -> aucune expression aval a changer.\nreturn $input.all().map(i => ({ json: { ...(i.json.fields || {}), id: i.json.id } }));"
      },
      "id": "rc-render-cfg-01f",
      "name": "Airtable — Lire Config Render",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        414,
        360
      ]
    },
    {
      "id": "http-rsc-01",
      "name": "HTTP — renderSocialCard",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        1150,
        300
      ],
      "continueOnFail": true,
      "parameters": {
        "method": "POST",
        "url": "={{ $json.render_url }}",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "X-Render-Key",
              "value": "={{ $json.render_key }}"
            }
          ]
        },
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify($json.payload) }}",
        "options": {
          "timeout": 100000
        }
      },
      "onError": "continueRegularOutput",
      "alwaysOutputData": true
    },
    {
      "id": "finalize-01",
      "name": "Finalize carte",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1350,
        300
      ],
      "parameters": {
        "mode": "runOnceForEachItem",
        "jsCode": "// Réapparie la réponse du moteur avec sa demande (le nœud HTTP conserve l'ordre et le nombre\n// d'items, y compris quand il échoue). Un rendu raté ressort sans URL plutôt qu'en exception :\n// il doit coûter une carte, pas le workflow entier.\nconst b = $('Build — URL publique').all()[$itemIndex].json;\nconst resp = $input.item.json || {};\nconst url = (typeof resp.url === 'string' && resp.url) ? resp.url : '';\nconst err = url ? '' : String((resp.error && (resp.error.message || resp.error)) || resp.message || 'rendu indisponible').slice(0, 200);\nreturn { json: { id: b.id, image_index: b.image_index, total_images: b.total_images,\n                 carte: b.carte, public_url: url, erreur: err } };"
      }
    },
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "mm-run-visuel",
        "authentication": "headerAuth",
        "responseMode": "onReceived",
        "options": {}
      },
      "name": "Webhook — mm-run-visuel",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2,
      "position": [
        0,
        160
      ],
      "webhookId": "mm-run-visuel",
      "credentials": {
        "httpHeaderAuth": {
          "id": "jSMRNljvTwk5b77f",
          "name": "MM Trigger"
        }
      },
      "id": "3292986d-d3fd-453e-bb53-d3ed6e42fda6"
    }
  ],
  "connections": {
    "Cron — Toutes 2h 8-20h": {
      "main": [
        [
          {
            "node": "Airtable — Lire Config Render (NocoDB)",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Airtable — Lire rédigés": {
      "main": [
        [
          {
            "node": "Parse — Cartes",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Build — URL publique": {
      "main": [
        [
          {
            "node": "HTTP — renderSocialCard",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Collect — URLs par contenu": {
      "main": [
        [
          {
            "node": "Airtable — Status=prêt_à_valider",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Airtable — Lire Config Render": {
      "main": [
        [
          {
            "node": "Airtable — Lire VALIDATION_MODE (NocoDB)",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Airtable — Lire VALIDATION_MODE": {
      "main": [
        [
          {
            "node": "Airtable — Lire rédigés (NocoDB)",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "HTTP — renderSocialCard": {
      "main": [
        [
          {
            "node": "Finalize carte",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Finalize carte": {
      "main": [
        [
          {
            "node": "Collect — URLs par contenu",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Webhook — mm-run-visuel": {
      "main": [
        [
          {
            "node": "Airtable — Lire Config Render (NocoDB)",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Airtable — Lire rédigés (NocoDB)": {
      "main": [
        [
          {
            "node": "Airtable — Lire rédigés",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Airtable — Lire VALIDATION_MODE (NocoDB)": {
      "main": [
        [
          {
            "node": "Airtable — Lire VALIDATION_MODE",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Airtable — Lire Config Render (NocoDB)": {
      "main": [
        [
          {
            "node": "Airtable — Lire Config Render",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Parse — Cartes": {
      "main": [
        [
          {
            "node": "Build — URL publique",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "settings": {
    "executionOrder": "v1",
    "binaryMode": "separate",
    "errorWorkflow": "MMERRALERT0001"
  },
  "nodeGroups": [],
  "tags": []
}
