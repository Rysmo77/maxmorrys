{
  "name": "@mm/api-worker",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Worker maxmorrys-api — protocole onCall sur api.maxmorrys.me, avec relais vers Cloud Functions",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --env=\"\"",
    "deploy:preview": "wrangler deploy --env preview"
  },
  "dependencies": {
    "@mm/firebase-auth-rest": "*",
    "@mm/firestore-rest": "*",
    "@mm/gcp-auth": "*",
    "@mm/shared": "*"
  }
}
