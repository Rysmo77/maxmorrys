{
  "name": "maxmorrys-workers",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Monorepo des Cloudflare Workers maxmorrys.me (media, site, api, jobs, render)",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit && tsc --noEmit -p tsconfig.test.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.20.1",
    "@cloudflare/workers-types": "^5.20260730.1",
    "@types/node": "^26.1.2",
    "typescript": "^5.6.3",
    "vitest": "^4.1.0",
    "wrangler": "^4.20.0"
  }
}
